import { Categories, Headline, Products, Screen } from 'components';

const SearchResults = () => {
    return (
        <Screen>
            <Headline />
            <Categories />
            <Products />
        </Screen>
    );
};

export default SearchResults;
